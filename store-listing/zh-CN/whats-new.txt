本次更新聚焦 Password Vault 安全加固与 iframe 支持。

- Password Vault：主密码改为在扩展专用窗口中输入，页面脚本不再能观测主密码（不会再在页面内输入）。
- 借助记录时采集的 frame 信息，flow 步骤现在可以定位 iframe 内的元素。
- 长时间运行的 flow 在浏览器挂起后台工作进程时不会再卡住，会被安全地标记为失败。
- 加强保管库加密（PBKDF2 提升到 60 万次迭代，AES 密钥在 Service Worker 内保持不可导出）。

另外还包含侧边栏与 flow 运行体验的细节优化。
