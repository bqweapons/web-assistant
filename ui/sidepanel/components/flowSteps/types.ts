import type { FlowDataSourceMeta, FlowStepData, FlowStepField } from '../../../../shared/flowStepMigration';

export type StepFieldOption = {
  value: string;
  label: string;
};

export type StepField = FlowStepField;
export type DataSourceMeta = FlowDataSourceMeta;
export type StepData = FlowStepData;
